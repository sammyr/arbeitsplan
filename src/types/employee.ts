export interface Employee {
  id: string;
  firstName: string;
  lastName?: string;
  email: string;
  mobilePhone: string;
  role?: string;
  storeId?: string;
  organizationId: string;
  createdAt: string;
  updatedAt: string;
}
